import { GlassCardDirective } from './glass-card.directive';

describe('GlassCardDirective', () => {
  it('should create an instance', () => {
    const directive = new GlassCardDirective();
    expect(directive).toBeTruthy();
  });
});
